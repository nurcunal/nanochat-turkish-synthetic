This folder is generated by `synthetic/prepare_accepted_sets.py`.

It contains ONLY judge-accepted synthetic EVAL benchmarks in nanochat CustomJSON JSONL format.

IMPORTANT:
- These are held-out evaluation sets and MUST NOT be used for training.
- Use them only for evaluation/benchmarking (e.g., report synthetic benchmark accuracy).

